server {
    listen 80;
    server_name _;

    # Re-resolve Docker service DNS so nginx keeps working after php container recreation.
    resolver 127.0.0.11 valid=30s ipv6=off;

    root /var/www/app/public;
    index index.php;

    location / {
        try_files $uri /index.php$is_args$args;
    }

    location ~ \.php$ {
        include fastcgi_params;
        set $php_upstream php:9000;
        fastcgi_pass $php_upstream;
        fastcgi_param SCRIPT_FILENAME /var/www/app/public$fastcgi_script_name;
        fastcgi_param DOCUMENT_ROOT /var/www/app/public;
        # Preserve upstream TLS / client info when behind Caddy or another reverse proxy
        fastcgi_param HTTP_X_FORWARDED_PROTO $http_x_forwarded_proto;
        fastcgi_param HTTP_X_FORWARDED_FOR $http_x_forwarded_for;
        fastcgi_param HTTP_X_FORWARDED_HOST $http_x_forwarded_host;
        internal;
    }

    location ~* \.(?:css|js|jpg|jpeg|gif|png|svg|ico|woff2?)$ {
        expires 7d;
        access_log off;
        try_files $uri /index.php$is_args$args;
    }

    client_max_body_size 32m;
}
